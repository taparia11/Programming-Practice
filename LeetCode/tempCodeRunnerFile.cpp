#include<bits/stdc++.h>
#include<vector>
using namespace std;

void xback(string s, string ans){
    if(s.length()==0)
        {
            cout<<ans<<endl;
            return;
        }

    string 

    if(s[0] == ns[0])
        return ns;
    return s[0] + ns;

} 
int main(){
    cout<<xback("aabbbc");
    return 0;
}